<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- Arka planı şeffaf yap -->
    <stroke
        android:width="2dp"
        android:color="@android:color/black" /> <!-- Siyah kenarlık -->
    <corners android:radius="@dimen/card_radius"/> <!-- Köşeleri yuvarla -->
</shape>